name: Build & Push Multi-Service Images

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'

jobs:
  build:
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        service:
          - name: circlewash
            context: ./circlewash
            environment:
              MONGO_URI: ${{ vars.MONGO_URI }}
              PORT: ${{ vars.CIRCLEWASH_LEGACY_PUBLIC_FACING_PORT }}
            dockerfile: ./circlewash/dockerfile.prod
          - name: circlewashstaff
            context: ./circlewashstaff
            environment:
              MONGO_URI: ${{ vars.MONGO_URI }}
              PORT: ${{ vars.CIRCLEWASH_ADMIN_PORTAL_PORT }}
            dockerfile: ./circlewashstaff/dockerfile.prod

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push ${{ matrix.service.name }}
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.dockerfile }}
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/${{ vars.REPOSITORY_LOWER_NAME }}-${{ matrix.service.name }}:latest